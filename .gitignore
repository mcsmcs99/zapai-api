# --- Node / npm ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
# (se quiser versionar o lockfile, remova a linha acima)

# --- Logs & pids ---
logs/
*.log
*.pid
*.seed
*.pid.lock

# --- Coverage / build / cache ---
coverage/
dist/
build/
.cache/
.tmp/
.tmp/*
.temp/
.nyc_output/

# --- Env files (não commitar segredos) ---
.env
.env.*
!.env.example

# --- Sistema / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- Docker (artefatos locais) ---
docker/mysql/data/
docker/mysql/init/*.sql~
docker-compose.override.yml

# --- Uploads temporários (se usar) ---
uploads/*
!uploads/.gitkeep
tmp/
temp/

# --- Sequelize ---
# Mantém o config JS e ignora o JSON legado
config/config.json
!config/config.js
# Arquivos gerados temporários
sequelize-meta.json

# --- Swagger (se gerar dinamicamente) ---
# Descomente se o openapi.json for gerado em runtime e você NÃO quiser versionar
# openapi.json

# --- Outros (ajuste conforme necessidade) ---
*.local
*.orig
